// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/anaconda
{
	"name": "Python3 Cuda with Pixi",
	"build": { 
		"context": "..",
		"dockerfile": "Dockerfile"
	},

	"runArgs": [
		"--platform=linux/amd64" 
	],

	"mounts": [
  		"source=${localEnv:PIXI_CACHE_DIR},target=/home/vscode/.cache/rattler,type=bind,consistency=cached"
	],
	"customizations": {
		"vscode": {
			"extensions": [
				"tamasfe.even-better-toml",
				"ms-python.python"
			]
		}
	},

	"postAttachCommand": "sudo chown -R vscode /workspaces",
	
	"remoteEnv": {
		"LD_LIBRARY_PATH": "${containerEnv:LD_LIBRARY_PATH}:/home/vscode/.mujoco/mujoco210/bin"
	}

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Use 'postCreateCommand' to run commands after the container is created.
	// "postCreateCommand": "python --version",

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "nvidia"
}
